# BESS HarmonyOS 0.2.0

- 首个纯血鸿蒙手机版本，基线 HarmonyOS 6.0.0（API 20）。
- 完整提供词汇五类题型、FSRS 复习、场景逐步揭示、文章音频与双语字幕、学习设置和本地提醒。
- 支持 `.besspack` schema 3、`.bessarticle` schema 2 和 `.bessbackup` v1。
- 可与 Android / Windows 手动迁移学习记录；恢复为完整覆盖，不做合并或自动同步。
- 完全离线，无账号、服务器、遥测、WebView 或网络权限。
