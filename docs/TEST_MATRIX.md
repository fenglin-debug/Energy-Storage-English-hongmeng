# HarmonyOS 验收矩阵

## 0.4.1 自动化

- ArkTS 单元测试：FSRS 共享金向量、未加密/加密备份往返、Unicode 密码、错误密码、密文篡改、未知格式。
- 文件安全：路径穿越、重复条目、截断 ZIP、超限条目、解压总量、JSON 深度和校验不一致。
- ArkData：首次建库、单事务恢复回滚、拒绝高版本数据库、未来 schema 1→2 迁移框架。
- 静态契约：0.4.1/versionCode 5、API 20、离线权限、无 WebView、两个文章包共 22 篇及全部锁定资源 SHA-256。
- 词汇：搜索覆盖单词/中英文释义/别名；未学、学习中、已掌握、到期、收藏组合筛选；收藏占位与详情。
- 学习状态机：新词直接展示、复习先回忆、旧 `TRANSFER` 兼容、评价可改、下一条单次落库、重复点击不增加日志、无限到期修复和精确断点。
- 场景：客户音频门槛、中英原文同步揭示、隐藏态不创建中英文文本组件。
- 文章：双包幂等安装和进度保留、普通循环、随机整袋无重复、重洗首条避开当前、自动续播开关、上一首/下一首和后台进度保存。

## 三端互操作

使用固定时间与 Unicode 密码 `储能-pass-2026` 生成样本，分别执行：

- Android → HarmonyOS、HarmonyOS → Android
- Windows → HarmonyOS、HarmonyOS → Windows
- Android → Windows、Windows → Android（既有测试持续保留）

每个方向覆盖加密、未加密、空数据、大量日志、中文、特殊字符、精确断点和语料差异。恢复前记录九类数量，恢复后逐表比对。拒绝样本后再次逐表比对，确认数据库未改变。

## API 20 模拟器

- 只清除 `BESS_API20` 中 `com.bess.salestrainer` 的应用数据，不重置模拟器。
- 安装现有 0.2.2，创建收藏、已掌握词、未完成词汇/情景和文章进度，再用同一签名覆盖安装 0.4.1。
- 验证旧数据保留、旧无限到期修复、词库浏览、复习流程、情景中英揭示和 22 篇文章。
- 验证自动下一篇开关、普通循环、随机无重复、后台播放、锁屏/耳机切歌和进度恢复。
- 飞行模式完成核心流程，确认没有网络、麦克风或 WebView 权限。

## 本次不在范围

- HarmonyOS 6.1 / API 24 真机、AppGallery 上传和安卓截图 0.98 相似度验收。

## 发布门禁

- `bundleName=com.bess.salestrainer`，版本号符合台账且 `versionCode` 递增。
- Release 非调试、APP/HAP 已用永久发布证书签名，指纹与台账一致。
- 只声明手机、不申请 INTERNET、不包含 WebView/联网运行时。
- Android 三份内置资源与 `assets.lock.json` SHA-256 一致，两个文章包合计 22 篇。
- `SHA256SUMS.txt` 可验证最终 APP/HAP。
